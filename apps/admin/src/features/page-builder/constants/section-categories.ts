/** The Page Builder's "Sections" quick-filter chips — real Pattern
 * `category` values, not a second taxonomy. A Pattern saved under any
 * other (or no) category is still reachable via the "Patterns" tab's
 * full browse — this list is only a beginner-friendly shortcut onto the
 * same data (Milestone 6 spec's own example category list). */
export const SECTION_CATEGORIES = [
  'Hero',
  'Features',
  'About',
  'Services',
  'Testimonials',
  'Pricing',
  'FAQ',
  'CTA',
  'Contact',
] as const;
