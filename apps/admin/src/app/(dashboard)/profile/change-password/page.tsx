import { ChangePasswordPageContent } from '@/features/profile';

/** Self-service — no permission requirement. */
export default function ChangePasswordPage() {
  return <ChangePasswordPageContent />;
}
