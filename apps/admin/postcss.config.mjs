/** Tailwind v4 CSS-first setup — approved decision, docs/57_DESIGN_SYSTEM.md /
 * docs/58_FRONTEND_FOLDER_STRUCTURE.md. The legacy config/tailwind/tailwind.config.js
 * (Tailwind v3 syntax) is deliberately not referenced here — left untouched. */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
