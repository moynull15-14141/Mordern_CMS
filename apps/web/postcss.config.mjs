/** Tailwind v4 CSS-first setup, mirroring apps/admin's own
 * postcss.config.mjs (docs/57_DESIGN_SYSTEM.md). */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
